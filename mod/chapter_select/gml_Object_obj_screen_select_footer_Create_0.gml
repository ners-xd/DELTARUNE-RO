/// PATCH

/// REPLACE
    var language_text = (global.lang == "en" ? "日本語" : "English")
/// CODE
    var language_text = (global.lang == "en") ? "日本語" : "Română";
/// END

/// REPLACE
        var quit_text = (global.lang == "en" ? "Quit" : "終了")
/// CODE
        var quit_text = (global.lang == "en") ? "Ieși" : "終了";
/// END